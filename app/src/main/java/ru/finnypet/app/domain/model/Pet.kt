package ru.finnypet.app.domain.model

/**
 * Питомец целиком: обратимое состояние и накопленный рост.
 *
 * ТЗ разводит эти два понятия (состояние меняется после отдельного решения,
 * стадия — по совокупности решений за несколько периодов), но у них один
 * владелец и одна строка в хранилище, поэтому наружу они отдаются парой.
 */
data class Pet(
    val state: PetState,
    val growth: PetGrowth,
)
